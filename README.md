# Theorema Aureum 143 — Morning Star YM Tower

Classical trio only. No axioms.

Axiom footprint: `{propext, Classical.choice, Quot.sound}`.
Zero `sorry`. Zero `native_decide`. Mathlib v4.12.0 only.

## What is here

Machine-checked Lean 4 proofs forming the YM coupling-constant tower for
the Kotecky–Preiss criterion applied to SU(3) lattice Yang–Mills at coupling
β₀ ∈ (2.07, 2.08).

### Proved (0 sorry, classical trio)

| Result | File | Status |
|--------|------|--------|
| β₀ ∈ (2.07, 2.08) | `Towers/YM/Wall256_Scaffold.lean` | BRICK |
| Bessel enclosure tsum ≤ det | `Towers/YM/BesselBounds.lean` | BRICK |
| W1 numeric bound w1 ≤ 107/756 | `Towers/YM/BesselBounds.lean` | BRICK |
| 107/756 < 1/7 | `Towers/YM/WeylToeplitzBound.lean` | BRICK |
| KP gap_kp_star > 2 | `Towers/YM/KP_Closure.lean` | BRICK |
| YM collection entry-point | `Towers/YM/YMCollection.lean` | WIRED |

### Open surfaces (named hypotheses, not `sorry`)

| Surface | Reason open |
|---------|-------------|
| `SzegoGap` — w1(β₀) = w1_weyl_series(β₀) | SU(3) Gross–Witten formula absent from Mathlib v4.12.0 |
| `W1_KP_Surface` | SU(3) Haar integral absent |
| `Hw1_Surface` | SU(3) Haar integral absent |
| YM Surface #1 (mass gap ρ < 1) | **LOCKED OPEN** — continuum limit not constructed |

## Structure

```
Towers/YM/         — 185 Lean files: full KP chain + Wall256 bridge
Towers/RH/Chain/   — C01–C10 Riemann Hypothesis conditional chain
KP/                — standalone KP certificate (Arb-based)
lakefile.lean      — Mathlib v4.12.0, lean_lib Towers + KP + RH
lean-toolchain     — leanprover/lean4:v4.12.0
```

## Reproduce

```bash
# Requires elan with Lean 4.12.0
lake update
lake exe cache get   # fast: downloads precompiled Mathlib oleans
lake build
grep -rn 'sorry' Towers/ KP/ RH/  # should return nothing
```

## Honesty statement

This repository does **not** claim to solve the Clay Yang–Mills Mass Gap
problem. YM Surface #1 (`ρ < 1`, i.e. a genuine spectral gap in the
continuum limit) is locked OPEN. The KP chain certifies a conditional
input (`w1(β₀) < 1/7`) to the OS4 cluster criterion under a named
open hypothesis (`SzegoGap`). No mass gap, no `μ > 0`, no Clay claim.

## Certificate

`FOR_CERN.txt` — SHA-256 manifest, auto-generated by GitHub Actions on
every push.  See the Actions tab for the latest run.
